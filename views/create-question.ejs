<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <meta
      name="description"
      content="Purpose Application UI is the following chapter we've finished in order to create a complete and robust solution next to the already known Purpose Website UI."
    />
    <meta name="author" content="Webpixels" />
    <title>Trainity Dashboard</title>
    <!-- Favicon -->
    <!-- Favicon -->
    <link rel="icon" href="../img/custom/favicon-32x32.jpg" type="image/png" />
    <!-- Font Awesome 5 -->
    <link
      rel="stylesheet"
      href="../../libs/@fortawesome/fontawesome-free/css/all.min.css"
    />
    <!-- Page CSS -->
    <!-- <link
      rel="stylesheet"
      href="../../libs/quill/dist/quill.core.css"
      type="text/css"
    /> -->
    <link rel="stylesheet" href="//cdn.quilljs.com/1.3.6/quill.bubble.css" />
    <link rel="stylesheet" href="../../libs/select2/dist/css/select2.min.css" />
    <link rel="stylesheet" href="../../libs/flatpickr/dist/flatpickr.min.css" />
    <!-- Purpose CSS -->
    <link rel="stylesheet" href="../../css/purpose.css" id="stylesheet" />
    <style>
      .option-div {
        display: grid;
        grid-template-columns: 1fr auto;
        gap: 10px;
        margin-bottom: 1rem;
      }
    </style>
    <!-- Hotjar Tracking Code for Admin Website -->
<script>
  (function(h,o,t,j,a,r){
      h.hj=h.hj||function(){(h.hj.q=h.hj.q||[]).push(arguments)};
      h._hjSettings={hjid:3534926,hjsv:6};
      a=o.getElementsByTagName('head')[0];
      r=o.createElement('script');r.async=1;
      r.src=t+h._hjSettings.hjid+j+h._hjSettings.hjsv;
      a.appendChild(r);
  })(window,document,'https://static.hotjar.com/c/hotjar-','.js?sv=');
</script>
  </head>

  <body class="application application-offset">
    <div
      class="modal fade fixed-right"
      id="modal-products"
      tabindex="-1"
      role="dialog"
      aria-hidden="true"
    >
      <div class="modal-dialog modal-vertical" role="document">
        <div class="modal-content">
          <div class="scrollbar-inner">
            <div class="modal-body"></div>
          </div>
        </div>
      </div>
    </div>
    <!-- Application container -->
    <div class="container-fluid container-application">
      <!-- Sidenav -->
      <%- include('partials/sidepanel'); -%>
      <!-- Content -->
      <div class="main-content footer-margin-mobile position-relative">
        <!-- Main nav -->
        <%- include('partials/navbar'); -%>

        <!-- Page content -->
        <div class="page-content">
          <div class="row justify-content-center">
            <div class="col-lg-8">
              <!-- Page title -->
              <div class="page-title">
                <div class="row justify-content-between align-items-center">
                  <div
                    class="col-md-6 d-flex align-items-center justify-content-between justify-content-md-start mb-3 mb-md-0"
                  >
                    <!-- Page title + Go Back button -->
                    <div class="d-inline-block">
                      <h5
                        class="h4 d-inline-block font-weight-400 mb-0 text-white"
                      >
                        Create Question
                      </h5>
                    </div>
                    <!-- Additional info -->
                  </div>
                </div>
              </div>
              <%- include('partials/utility-modals'); -%>
              <form class="mt-4">
                <div class="card">
                  <div class="card-body">
                    <div class="form-group">
                      <label class="form-control-label">Question</label>
                      <textarea
                        class="form-control"
                        name="question"
                        placeholder="Enter Question Description"
                        rows="2"
                        resize="none"
                      ></textarea>
                    </div>

                    <div class="form-group">
                      <label class="form-control-label">Estimated Time</label>
                      <input
                        type="number"
                        name="estimatedTime"
                        placeholder="10 (in minutes)"
                        class="form-control"
                        required
                      />
                    </div>

                    <div class="form-group">
                      <label class="form-control-label">Quiz ID</label>
                      <select name="quizID" class="custom-select">
                        <option selected>Open this select menu</option>
                        <% for(var quiz of quizList) { %>
                        <option value="<%= quiz._id %>">
                          <%= quiz.title %>
                        </option>
                        <% } %>
                      </select>
                    </div>

                    <div class="form-group">
                      <label class="form-control-label">Question ID</label>
                      <input
                        type="number"
                        name="questionID"
                        class="form-control"
                        required
                      />
                    </div>

                    <div class="form-group">
                      <div
                        style="
                          display: grid;
                          grid-template-columns: 1fr auto;
                          margin-bottom: 1rem;
                        "
                      >
                        <label class="form-control-label">Options</label>
                        <button
                          type="button"
                          class="btn btn-sm btn-primary"
                          onclick="addOption()"
                        >
                          Add Option
                        </button>
                      </div>
                      <div id="optionList">
                        <div class="option option-div">
                          <div>
                            <textarea
                              class="form-control option-description"
                              placeholder="Enter Option"
                              rows="2"
                              resize="none"
                            ></textarea>
                          </div>
                          <div style="align-items: center">
                            <button
                              type="button"
                              class="btn btn-danger"
                              onclick="removeOption(this)"
                            >
                              Remove
                            </button>
                          </div>
                        </div>
                      </div>
                    </div>

                    <div class="form-group">
                      <div class="mt-4">
                        <label class="form-control-label">Question Image</label>
                        <input
                          type="file"
                          name="question-image"
                          id="question-image"
                          class="custom-input-file custom-input-file--2"
                          data-multiple-caption="{count} files selected"
                        />
                        <label for="question-image">
                          <i class="fa fa-upload"></i>
                          <span>Choose a file…</span>
                        </label>
                      </div>
                    </div>

                    <!-- Button trigger modal -->
                    <button type="submit" class="btn btn-primary">
                      Create
                    </button>
                  </div>
                </div>
              </form>
            </div>
          </div>
        </div>
        <!-- Footer -->
        <%- include('partials/footer'); -%>
      </div>
    </div>
    <!-- Scripts -->
    <!-- Core JS - includes jquery, bootstrap, popper, in-view and sticky-kit -->
    <script src="../../js/purpose.core.js"></script>
    <!-- Page JS -->
    <script src="../../libs/dropzone/dist/min/dropzone.min.js"></script>
    <script src="../../libs/bootstrap-tagsinput/dist/bootstrap-tagsinput.min.js"></script>
    <!-- <script src="../../libs/quill/dist/quill.min.js"></script> -->
    <script src="//cdn.quilljs.com/1.3.6/quill.js"></script>
    <script src="../../libs/select2/dist/js/select2.min.js"></script>
    <script src="../../libs/flatpickr/dist/flatpickr.min.js"></script>
    <!-- Purpose JS -->
    <script src="../../js/purpose.js"></script>
    <!-- Demo JS - remove it when starting your project -->
    <script src="../../js/demo.js"></script>

    <script>
      function addOption() {
        var optionList = document.getElementById("optionList");
        var optionInput = document.createElement("div");
        optionInput.classList.add("option");
        optionInput.classList.add("option-div");
        optionInput.innerHTML = `
          
                        <div>
                            <textarea
                              class="form-control option-description"
                              placeholder="Enter Option"
                              rows="2"
                              resize="none"
                            ></textarea>
                          </div>
                          <div style="align-items: center">
                            <button
                              type="button"
                              class="btn btn-danger"
                              onclick="removeOption(this)"
                            >
                              Remove
                            </button>
                          </div>
                          
            `;
        optionList.appendChild(optionInput);
      }

      function removeOption(button) {
        const element = button.parentNode.previousElementSibling;
        const parent = button.parentNode;
        element.remove();
        parent.remove();
      }
    </script>

    <script>
      const form = document.querySelector("form");

      form.addEventListener("submit", async (e) => {
        e.preventDefault();
        const quizID = form.quizID.value;
        const questionID = form.questionID.value;
        const estimatedTime = form.estimatedTime.value;
        const question = form.question.value;
        const questionImageFile =
          document.querySelector("#question-image").files[0];
        const questionImageFileName = questionImageFile.name;
        const questionImageContentType = questionImageFile.type;
        const uploadType = "learning";

        const optionDivs = document.querySelectorAll(".option");
        const options = [];

        for (let i = 0; i < optionDivs.length; i++) {
          const value = optionDivs[i].querySelector("textarea").value;
          if (value.length > 0)
            options.push({
              value,
            });
        }

        console.log(options);

        $("#loadingModal").modal("show");
        $("#errorModal").modal("hide");
        $("#successModal").modal("hide");

        const urlRes = await fetch(
          `/aws/get/preSignedUrl?fileName=${questionImageFileName}t&contentType=${questionImageContentType}&uploadType=${uploadType}`,
          {
            method: "GET",
          }
        );
        const urlData = await urlRes.json();
        console.log(urlData);

        try {
          const response = await fetch(urlData.url, {
            method: "PUT",
            headers: {
              "Content-Type": questionImageContentType,
            },
            body: questionImageFile,
          });
          if (response.status === 200 && response.url) {
            const res = await fetch("/learning/create/question", {
              method: "POST",
              body: JSON.stringify({
                quizID,
                questionID,
                question,
                estimatedTime,
                options,
                questionImage: `https://dvfkpf9qxfl09.cloudfront.net/${urlData.key}`,
              }),
              headers: { "Content-Type": "application/json" },
            });

            const data = await res.json();

            if (data._id) {
              $("#loadingModal").modal("hide");
              $("#errorModal").modal("hide");
              $("#successModal").modal("show");
            }
          } else {
            $("#loadingModal").modal("hide");
            $("#errorModal").modal("show");
            $("#successModal").modal("hide");
          }
        } catch (error) {
          $("#loadingModal").modal("hide");
          $("#errorModal").modal("show");
          $("#successModal").modal("hide");
          console.log(error);
        }
      });
    </script>
  </body>
</html>
